-- Generated by xeneon-edge-agents. Do not add monitor layout here:
-- hyprmoncfg owns ~/.config/hypr/monitors.lua.
--
-- This is deliberately a per-device mapping. A missing output does not
-- authorize mapping this touchscreen across the desktop.
hl.device({
  name = "@TOUCH_DEVICE@",
  output = "@OUTPUT_CONNECTOR@",
})
